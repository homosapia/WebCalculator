@model List<OperationModel>

<div>
    @foreach (OperationModel operation in Model)
    {
        <button class="operator" style="background-color: @operation.Сolor.ToString();" data-operator="@operation.OpetationType">
            @operation.OpetationType
        </button>
    }
</div>